# TestPilot — Claude Code Instructions

## What This Project Is

TestPilot is a Kotlin Multiplatform (KMM) SDK for AI-driven mobile app testing using vision (screenshots). It supports two modes: `./testpilot analyze` (exploratory UX analysis → HTML report) and `./testpilot test` (deterministic pass/fail → CI exit codes). Supports iOS (XCTest) and Android (UIAutomator).

## Tech Stack

- **Core SDK**: Kotlin Multiplatform 1.8, Gradle (JDK 11+)
- **AI client**: `com.aallam.openai:openai-client:3.1.1` (aallam)
- **HTTP**: Ktor 2.2.4
- **iOS wrapper**: Swift + SPM, consumes KMM-built XCFramework via cinterop
- **Android**: UIAutomator 2.2.0, min SDK 29

## Project Structure

```
sdk/testpilot/src/
  commonMain/   ← Platform-agnostic logic: Analyst, TestAnalyst, prompts, Config
  androidMain/  ← UIAutomator-backed snapshot + actor
  iosMain/      ← XCTest cinterop + .def files, iOS snapshot + actor, CachingAIClient
swift-wrapper/  ← Thin Swift API on top of KMM XCFramework
scripts/        ← build_ios_sdk.sh
mac-app/        ← macOS companion app (SwiftUI)
```

## Build Commands

```bash
# Build iOS XCFramework (run from repo root)
scripts/build_ios_sdk.sh

# Build Android AAR
cd sdk && ./gradlew testpilot:assembleDebug

# Build release XCFramework
scripts/build_ios_sdk.sh testpilot:assembleTestPilotSharedReleaseXCFramework
```

## Key Files

### Analyze mode (commonMain)
- `sdk/testpilot/src/commonMain/…/analyst/Analyst.kt` — explore loop (screenshot → AI → action → repeat)
- `sdk/testpilot/src/commonMain/…/ai/VisionPrompt.kt` — prompt for exploratory analysis
- `sdk/testpilot/src/commonMain/…/analyst/HtmlReportWriter.kt` — HTML report with inline screenshots

### Test mode (commonMain)
- `sdk/testpilot/src/commonMain/…/analyst/TestAnalyst.kt` — deterministic loop; terminates on Pass/Fail
- `sdk/testpilot/src/commonMain/…/ai/TestVisionPrompt.kt` — prompt for pass/fail evaluation
- `sdk/testpilot/src/commonMain/…/analyst/TestResult.kt` — `data class TestResult(passed, reason, steps)`

### Shared
- `sdk/testpilot/src/commonMain/…/analyst/AnalysisAction.kt` — sealed class: Tap/Scroll/Type/Done/Pass/Fail
- `sdk/testpilot/src/commonMain/…/runtime/Config.kt` — ConfigBuilder API

### Android (androidMain)
- `sdk/testpilot/src/androidMain/…/analyst/AnalystAndroid.kt` — analyze entrypoint; saves HTML report
- `sdk/testpilot/src/androidMain/…/analyst/TestAnalystAndroid.kt` — test entrypoint; emits TESTPILOT_STEP:/TESTPILOT_RESULT: markers

### iOS (iosMain)
- `sdk/testpilot/src/iosMain/…/analyst/AnalystIOS.kt` — analyze entrypoint; saves HTML report
- `sdk/testpilot/src/iosMain/…/analyst/TestAnalystIOS.kt` — test entrypoint; emits TESTPILOT_STEP:/TESTPILOT_RESULT: markers
- `sdk/testpilot/src/iosMain/…/ai/CachingAIClient.kt` — decorator: FNV-1a hash key, NSCachesDirectory store

### CLI
- `testpilot` — bash script: two subcommands (`analyze`, `test`), generates Swift harness, parses stdout markers

### macOS app
- `mac-app/TestPilotApp/Models/RunConfig.swift` — `RunMode` enum (analyze/test)
- `mac-app/TestPilotApp/Services/AnalysisRunner.swift` — parses TESTPILOT_STEP:/TESTPILOT_RESULT: in real-time
- `mac-app/TestPilotApp/Views/RunView.swift` — mode segmented control, conditional UI per mode
- `mac-app/TestPilotApp/Views/RunningView.swift` — verdict banner + step list for test mode

### Docs
- `docs/DEVELOPMENT.md` — full build & install guide
- `docs/arquitetura-analise-ux.md` — pt-BR technical architecture reference
- `docs/guia-analise-ux.md` — pt-BR user guide (non-technical)
- `docs/como-usar-analise-ios.md` — pt-BR usage guide

## Code Conventions

- Kotlin: standard KMM patterns, `expect`/`actual` for platform abstractions
- Swift: extensions on `XCTestCase`, `@MainActor` async
- Commit style: `type(scope): description` (e.g. `feat(KMM): add persistence api`)
- No tests directory in this repo — TestPilot IS the test framework

## Important Notes

- The `.def` files in `iosMain/` are generated by `build_ios_sdk.sh` from `.templ` files — do not edit `.def` files directly
- `Package.swift` references the XCFramework at a build output path; it won't resolve until the iOS SDK is built
- Config requires at minimum an `apiKey`; all other fields have defaults (maxTokens=200, maxSteps=40, temperature=0.0)
- Test mode stdout markers: `TESTPILOT_STEP: <message>`, `TESTPILOT_STEP: (cached) <message>`, `TESTPILOT_RESULT: PASS <reason>`, `TESTPILOT_RESULT: FAIL <reason>`
- `CachingAIClient` cache path: `NSCachesDirectory/testpilot-cache/` — scoped to the XCTest runner container, persists across separate runs of the same test target

## Mobbin Research Mode (mac-app)

- **Mobbin MCP endpoint**: `https://api.mobbin.com/mcp` — JSON-RPC 2.0, auth via Bearer token (OAuth PKCE against Supabase)
- **API hard limit**: `search_screens` accepts max `limit: 30`; passing 50+ returns a validation error
- **Always use `mode: "deep"`**: `mode: "fast"` returns semantically unrelated apps. Deep mode uses an AI pipeline that understands intent — critical for app-specific results
- **Query must include platform context**: `"Tesla iOS app onboarding"` returns 30x Tesla; `"Tesla onboarding"` returns 0 Tesla screens. The phrase "iOS app" is what anchors the deep-mode search to the right app
- **URL input**: `RunConfig.mobbinURL` accepts either a Mobbin URL or a plain app name. URL slug format: `{app-name}-{platform}-{uuid4}` — parsed to extract app name and platform automatically. Example: `https://mobbin.com/apps/tesla-ios-368d439c-.../...` → appName="Tesla", platform="ios"
- **Post-fetch filtering**: always fetches 30, filters by `app_name.contains(appName)`, returns up to `mobbinLimit`. Falls back to unfiltered if no match (e.g. unknown app name)
- **Parallel batching**: >10 screens → split into batches of 5, analyzed concurrently via `withThrowingTaskGroup`, then a text-only calibration call normalizes CRITICAL/ISSUE/POSITIVE severity across batches and generates the summary. ≤10 screens → single call (unchanged behavior)
